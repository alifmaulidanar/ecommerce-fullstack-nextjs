import React from 'react'
import FormLocation from '../_components/form-location'

export const runtime = "edge";

export default function CreatePage() {
  return (
    <FormLocation type='ADD' />
  )
}
